{
	"name": "agent-commerce-gateway",
	"version": "1.0.0",
	"private": true,
	"description": "The Intelligent Edge Gateway for Agent-Commerce-OS. Handles API Governance, Request Sanitization, and MCP aggregation.",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"wrangler": "^3.100.0"
	},
	"dependencies": {
		"hono": "^4.12.3"
	}
}