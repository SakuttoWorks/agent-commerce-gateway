{
	"name": "agent-commerce-gateway",
	"version": "1.0.0",
	"private": true,
	"description": "Secure Edge Gateway for Web Data Normalization & MCP Protocol Translation.",
	"license": "MIT",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"test": "vitest run",
		"test:e2e": "vitest run -c vitest.e2e.config.ts"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.13.5",
		"@cloudflare/workers-types": "^4.20260307.0",
		"typescript": "^6.0.0",
		"vitest": "^4.1.2",
		"wrangler": "^4.78.0"
	},
	"dependencies": {
		"hono": "^4.12.9"
	}
}