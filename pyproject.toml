[project]
dependencies = []
description = "Edge Gateway for Project GHOST SHIP"
name = "ghost-ship-proxy"
readme = "README.md"
requires-python = ">=3.12"
version = "1.0.0"

[tool.hatch.build.targets.wheel]
packages = ["src"]
